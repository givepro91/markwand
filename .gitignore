.nova/
node_modules/
dist/
out/
releases/
.vite/
build/icon-work/
*.local

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# 빌드 아티팩트
*.dmg
*.pkg
*.app

# 런치 미디어 (GIF / MP4 는 GitHub release asset 으로 관리, 원본 녹화 mov 는 민감)
*.mov
docs/launch/*.mp4
docs/launch/*.gif
docs/launch/*.png

# 환경변수
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# 로그
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# 캐시
.eslintcache
.cache/

# Nova Orbit — agent worktrees (do not commit)
.nova-worktrees/
.claude/worktrees/

# Bench baseline은 머신별로 달라서 로컬 비교용으로만 유지 (Plan §S3)
scripts/bench-transport.baseline.json
