# v0.4.0 UX Overhaul — Plan

> Mode: **deep** · Iterations: 1 · Generated: 2026-04-24 · Target release: `v0.4.0-beta.1`

---

## Context

v0.3.0-beta.9 공개 이후 남은 사용자 피드백 4건(#3 하이라이팅 · #4 이미지/ASCII 렌더 · #6 뷰어 리디자인 · #7 UX 친화 개편)과 `/nova:ux-audit` (2026-04-24) 가 5인 적대 평가자로 도출한 **Critical 8 / High 11 / Medium 11 / Low 2** 를 단일 **v0.4.0-beta.1** 릴리스에 담는다. Markwand 의 "개발자 전용 도구" 이미지를 탈피하고 PM·디자이너·파운더도 집어들 수 있는 마크다운 뷰어로 포지셔닝 재정립.

- **Scope IN**: UX Audit 40건 전체(C·H·M·L) + #3 annotation MVP + SSH 원격 프로젝트 목록 자동 갱신(SshPoller 확장)
- **Scope OUT (Scope Guard)**: v1.0 SSH IPC 분기(PR-A) · SSH annotation 쓰기 · annotation 4색 팔레트/note 패널(v0.5+) · v0.3 Known Gap M-2·M-7

---

## Problem

### UX Audit Critical (8)
| ID | 문제 | 파일:라인 |
|----|------|-----------|
| C1 | drift 전문용어 영문 전면 노출 (stale/missing/drift/ref) | `DriftPanel.tsx:24-34,236,251,347,353,518` · `FilterBar.tsx:21-33` · `ko.json:285-330` |
| C2 | 첫 화면 정체성 부재 (가치 명제 없음) | `App.tsx:366-372` · `ko.json:29-31` |
| C3 | 핵심 개념(워크스페이스/프로젝트/Inbox) 관계 설명 0 | `Sidebar.tsx:21-25` · `ko.json:128-138` |
| C4 | ProjectView 12 옵션 동시 노출 Miller 초과 | `ProjectView.tsx:456-512,640-670` · `FilterBar.tsx:192-304` |
| C5 | 사이드바 리사이즈 keyboard 불가 (WCAG 2.1.1/2.5.7) | `ProjectView.tsx:603-632` |
| C6 | 다크 탭 active 대비 **3.41:1 AA fail** (WCAG 1.4.3) | `themes.css:43 --accent:#388bfd` on white |
| C7 | `appendDocs` O(N) spread 매 chunk — 전역 re-render 폭발 | `state/store.ts:165-166` |
| C8 | InboxView scanDocs 이중 race (deps=projects 배열 참조) | `InboxView.tsx:66-134` |
| C9 | workspace 제거 시 host key 잔존 **GDPR 소지** | `main/ipc/workspace.ts:449-466` (`removeHostKey` 미호출) |

> Explorer C 재계산: `#388bfd` on white = **3.41:1 FAIL**, `#1f6feb` = **5.05:1 PASS**, `#58a6ff` = 2.42:1 FAIL(hover 만), `#79c0ff` = 1.82:1 FAIL. light `--text-muted #59636e` on `#eaeef2` = **4.84:1 PASS** (M3 Audit 과민 — 방어적 조정만).

### UX Audit High (11) · Medium (11) · Low (2)
요약: ErrorBoundary i18n 미연결(class 컴포넌트 제약) · humanizeError 단일 파일 고립 · Composer 영문 용어 · SSH 모달 8 필드 + focus trap 부재 · 터치 타겟 24px 경계 · DriftBadge/find aria-live 0 · App `docs` effect 폭발 · rehype-highlight eager(~1MB 번들) · img CLS(H9) · TOFU Trust 세션-only 0 · Composer 절대 경로 유출 · FilterBar 전체 재주사 · RecentDocsPanel 60s CLS · rememberHostKey 체크박스 0 · confirmshaming · drift 2MB silent skip

### 신규 기능
- **#3 하이라이팅 MVP**: 텍스트 선택 → 형광펜 적용 → `.md.anno.json` sidecar 저장/로드 → CSS Custom Highlight API 로 렌더. 로컬 md only(v0.4), SSH·다색·note 패널은 v0.5+.

### 인프라
- **SSH 원격 프로젝트 목록 자동 갱신**: 현재 SshPoller 가 `transport:status` 만 송신, `fs:project-change` 미구현. 디렉토리 레벨(depth ≤ 2) readdir diff 추가.

---

## Open Questions — 결정

| Q | 결정 | 근거 |
|---|------|------|
| **Q1 데이터 모델** | Sidecar JSON `<file>.md.anno.json` | git-friendly · 원본 무수정(drift 오탐 없음) · SSH 파일시스템 재사용 · frontmatter 는 `.md` 재파싱 + merge conflict 유발 |
| **Q2 저장 위치** | 원본 문서 디렉토리 옆 | 문서에 속한다는 멘탈 모델 · 외부 편집기·백업 공존 · 기존 FS9 write 권한 재사용 |
| **Q3 앵커링** | **text-quote 중심** (dom-anchor-text-quote + dom-anchor-text-position) — XPath 폴백 **없음** | rehype-sanitize 후 hljs span 삽입으로 XPath 경로 shift 심각 · 기존 `findInContainer.ts` TreeWalker→Range 로직 재활용 · ~5KB |
| **Q4 drift 접기** | **(a) 문제 있을 때만 표시** | C4 Miller 초과 해소 · 정상(0/0) 시 DriftBadge/DriftPanel 둘 다 hidden · `drift.*` i18n 키는 유지 |
| **Q5 SSH 프로젝트 갱신** | v0.4 포함 (Scope Guard 예외 명시) | SshPoller 내부 확장 + 기존 `fs:project-change` IPC 재사용 — M3+ IPC 계층에 닿지 않음 |

---

## Solution

### Sprint DAG

```
S0 ── Test Infra (jsdom + RTL, 0.5d) ──▶ [S2/S3/S6/S7 컴포넌트 테스트 DoD 를 블록]
                                         │
S1 ── Tokens/CLS/Perf quick-wins ────────┤
S2 ── Viewer Layout + a11y ──────────────┤
                                         ├─▶ S3 (i18n Overhaul + humanizeError) ─┐
                                         │                                        ├─▶ S5 (SSH Security) ─▶ S6 (SSH Wizard)
                                         │                                        │                               │
                                         │                                        └─▶ S4 (Perf Structure) ─▶ S7 (Annotation MVP)
                                         │                                                                        │
                                         └────────────────────────────────────────────────────────────────────────┴─▶ S9 Release
                                                                                                                  │
S8 ── SSH Auto-refresh (독립 언제든) ────────────────────────────────────────────────────────────────────────────┘
```

**병렬 가능**: S0 ∥ S1 ∥ S2 (파일 교집합 0) · S8 은 전 sprint 와 독립. 그 외는 ko.json / FilterBar / ComposerTray / DriftPanel / App.tsx / MarkdownViewer / SshHostKeyPrompt / SshWorkspaceAddModal 중복으로 **순차**.

---

### S0 — Test Infrastructure (jsdom + React Testing Library, 선행)

**스코프**: OR-4 옵션 A — renderer 컴포넌트 테스트 인프라 구축

| 파일 | 수정 |
|------|------|
| `package.json` devDeps | `@testing-library/react@^16` + `@testing-library/jest-dom@^6` + `@testing-library/user-event@^14` + `jsdom@^25` 추가 (happy-dom 은 react-i18next 호환 이슈로 제외) |
| `vitest.config.ts` | **단일 config + `environmentMatchGlobs`** — `[['src/renderer/**/*.test.tsx', 'jsdom'], ['**/*', 'node']]` 글로브 첫-매치 규칙으로 `.tsx` = jsdom, 나머지 `.ts` = node. `setupFiles: ['./vitest.setup.ts']` 공유 (node 환경에서는 DOM matcher 로드 skip). `@` alias 복제. (Evaluator Major-1 반영: projects 배열 대신 단순한 단일 config, 동작 동일) |
| **신규** `vitest.setup.ts` | `typeof window !== 'undefined'` 조건부 dynamic import 로 `@testing-library/jest-dom/vitest` + RTL `cleanup` 등록 (node 환경 DOM matcher 오염 방지 — Evaluator Major-2 반영) · matchMedia/ResizeObserver/IntersectionObserver polyfill · i18next `vi.mock` (key identity) |
| **신규** `src/renderer/__test-utils__/render.ts` | `renderWithProviders(ui, { theme, locale })` — i18next Provider + ThemeContext wrap |
| **신규** `src/renderer/__test-utils__/apiMock.ts` | `window.api` 전체 surface 의 vi.fn() 기본 구현 (prefs/get-set, workspace, project, fs, drift, ssh, annotation) |
| 스모크 테스트 | `src/renderer/components/ui/Button.test.tsx` 1건 — 새 인프라 동작 검증(sm size 28px / disabled / click) |

**DoD**:
- `pnpm test` 전체 회귀 0 (기존 250/260 baseline 유지)
- `pnpm test -- Button` 신규 스모크 테스트 PASS
- typecheck PASS · jsdom 환경에서 i18next 번역 렌더 확인
- 번들 영향 0 (devDeps only)

**이후 sprint 에서 컴포넌트 테스트 추가 DoD**:
- S2: Button sm hit-area 28px · aria-live · 키보드 리사이즈 onKeyDown 핸들러
- S3: I18nErrorBoundary 라벨 주입 · ko/en 전환
- S6: SSH Wizard Step1/Step2 전환 · focus trap
- S7: AnnotationToolbar 선택 이벤트 · anchor.test.ts 9 케이스

---

### S1 — Design Tokens · CLS · RecentDocsPanel (독립, 저위험)

**스코프**: C6 · H9 · M5 · M6 · M9 (+ M3 방어적 조정)

| 파일:라인 | 수정 |
|-----------|------|
| `themes.css:43` | 다크 `--accent: #388bfd` → **`#1f6feb`** (white text 5.05:1 PASS AA) |
| `tokens.css` | `--accent-contrast: #fff` 신규 토큰 추가 (selection / chip text 통일) |
| `globals.css:13` | `font-size:14px` → `var(--fs-md)` |
| `globals.css:50-51` | `::selection color:#fff` → `var(--accent-contrast)` |
| `globals.css:56-61` | find-highlight 색상은 CSS Highlight API 가 var 해석 불가 → **JS 생성 stylesheet** 로 `<style id="mw-highlight-theme">` 주입(`lib/highlightTheme.ts` 신규) |
| `DriftPanel.tsx:197,275,339,493` | `var(--bg-elev, #fafaf9)` fallback 삭제 · `borderRadius:'8px'` → `var(--r-lg)` |
| `MarkdownViewer.tsx:129-136,204-211` | img/SafeImage `onLoad` 전 기본 `aspect-ratio: 16/9` + `width: 100%` placeholder · `onLoad` 직후 `imgEl.style.aspectRatio = naturalWidth + '/' + naturalHeight` 로 swap · **단일 경로(JS 만), CSS `attr()` 규칙 미사용** |
| `markdown.css:164-168` | `.mermaid-block` `min-height: 240px` · `.markdown-body img { width: 100%; height: auto }` 유지 · **`aspect-ratio: attr(width)/attr(height)` 규칙 미사용** (Critic C-1: advanced `attr()` 는 Chrome 133+ / Electron 33 = Chromium 130 에서 미지원, CSS 선언이 JS swap 을 덮어쓸 위험 차단) |
| `RecentDocsPanel.tsx:75-78` | `setInterval(60s)` → 다음 자정까지 1회 재스케줄 (`msUntilMidnight()`) |
| `RecentDocsPanel.tsx:94-107` | `useMemo`에서 `docs.slice(0, 20)` 선행 후 classify |
| `RecentDocsPanel.tsx` 상대일 span | `min-width: 5ch; text-align: right` (라벨 폭 가변 CLS 차단) |

**DoD**:
- WCAG 1.4.3 다크 탭 active 5.05:1 (axe 수동 확인 or Colour Contrast Analyser)
- `pnpm dev` 에서 이미지 포함 md 로드 시 CLS = 0 (DevTools Performance 탭 시각 확인)
- **Electron 33 실측**: 이미지 `aspectRatio` 가 JS `onLoad` 후 swap 되는지 DevTools Elements 로 확인 (Chromium 130 에서 advanced `attr()` 미지원 → 반드시 JS 경로만 활성화)
- RecentDocsPanel 자정 경과 시 라벨 갱신 (수동 시계 조정 or 로컬 `Date` mock)
- typecheck PASS · 기존 vitest 회귀 0

---

### S2 — Viewer Layout 단순화 + Keyboard a11y (독립)

**스코프**: C4 · C5 · H5 · H6 (+ DriftBadge 조건부)

| 파일:라인 | 수정 |
|-----------|------|
| `ProjectView.tsx:603-632` | 리사이즈 핸들에 `tabIndex={0}` + `role="separator"` 유지 · `aria-orientation="vertical"` · `aria-valuenow={sidebarWidth}` `aria-valuemin={180}` `aria-valuemax={600}` · `onKeyDown`: ←(−10) / →(+10) / Shift+←(−50) / Shift+→(+50) / Home(180) / End(600). `prefs.set('sidebarWidth', v)` debounce 200ms |
| `FilterBar.tsx:192-304` | 기본 접기(`collapsed=true`) + 상단 단일 **"필터"** 버튼 + 활성 칩 count 배지. `prefs.filterBarCollapsed` 저장. 최소 노출 시 ≤ 5 요소(칩 배지 + 검색 input + 리셋) |
| `ui/Button.tsx:34-38` | `size='sm'` height 24 → **28px** min (`min-w/h: 28px`) |
| `ui/Toast.tsx:112-127` | close button `padding: var(--sp-1) var(--sp-2)` + `min-w/h: 28px` |
| `ComposerTray.tsx:117,119-126` | collapse "×" 버튼 `min-w/h: 28px` |
| `DriftBadge.tsx:40-79` | `role="status"` + `aria-live="polite"` · `missing + stale + ignored === 0` 시 `return null` (조건부 비표시) |
| `ProjectView.tsx:497-501` | find 카운터 wrapper에 `role="status"` + `aria-live="polite"` + `aria-atomic="true"` |

**DoD**:
- 키보드만으로 사이드바 너비 180 ↔ 600 조정 가능
- FilterBar 접힘 상태에서 가시 옵션 ≤ 5
- axe devtools 또는 `eslint-plugin-jsx-a11y` 로 새 규칙 위반 0
- sm 버튼 실제 렌더 시 hit-area 28×28 확인(DevTools)

---

### S3 — i18n Overhaul + humanizeError 공통화 (S1·S2 후)

**스코프**: C1 · C2 · C3 · H1 · H2 · H4 · M2 · L1 · L2 (+ ImageViewer FIT_MODES)

| 파일 | 수정 |
|------|------|
| **신규** `src/renderer/lib/humanizeError.ts` | `SshWorkspaceAddModal.tsx:40-52` 의 `humanizeError(t, message)` 추출 + drift/fs/readDoc 키셋 확장 (`error.drift.revalidateFailed`, `error.fs.readDocFailed`, `error.fs.tooLarge`) |
| **신규** `src/renderer/components/I18nErrorBoundary.tsx` | **패턴**: 기존 class `ErrorBoundary` 는 i18n 훅을 직접 사용 불가(class 컴포넌트 제약). class 에 `retryLabel, restartLabel, supportLabel, contentTitle, contentBody` 5개 라벨을 **prop** 으로 받도록 확장. 함수형 래퍼 `<I18nErrorBoundary>` 가 `const { t } = useTranslation()` 로 라벨 읽어 class 에 주입. `getDerivedStateFromError` / `componentDidCatch` 는 class 내부에 유지. 3단 CTA(다시 시도 · 앱 다시 시작 · 지원 링크) + `<details>` 로 원본 `error.message` 접기. `ProjectView.tsx:674,683,690` 사용처를 `<I18nErrorBoundary>` 로 교체. (Critic M-1) |
| `DriftPanel.tsx:24-34,236,251,347,353,518` | `STATUS_META` 라벨 i18n · "Finder" 버튼 `t('drift.openInFinder')` · 2MB skip 시 "N 파일이 크기 한도로 건너뜀" 문구(L2 해소) |
| `FilterBar.tsx:21-33` | `STATUS_LABELS`/`SOURCE_LABELS` 를 `t()` 호출로 전환 |
| `ImageViewer.tsx:23-27` | `FIT_MODES` 라벨 `t('imageViewer.fit/actual/fill')` |
| `ComposerTray.tsx:109,117,125` | "Composer Tray"/"Clear"/"×" → `t('composer.tray.aria')` / `t('composer.clearAll')` / `t('composer.collapse')` |
| `ComposerOnboarding.tsx:42,45,48,52` | "Context Composer" → 비개발자 카피 "한 번에 보내기" / "@ref" → "참조 링크" 등 카피 개편 |
| `App.tsx:366-372` | 빈 상태 타이틀·설명 가치 명제화: `t('empty.title')` = "마크다운 문서를 한 곳에서 읽어요", `t('empty.description')` + 예시 chip 2개 (".git 있는 프로젝트", "문서 폴더") |
| **신규** `App.tsx` onboarding 오버레이 | 첫 워크스페이스 추가 완료 직후 1회 표시 (`prefs.onboardingShown: true` 로 영속). 3 카드: "폴더 = 워크스페이스" / ".git / package.json = 프로젝트" / "ESC 로 닫기" |
| `App.tsx:297-301` | `handleAddWorkspace` 완료 직후 `toast.success(t('workspace.addedToast', { count: N }))` 추가 |
| `WorkspaceManageModal.tsx` confirm | `ko.json:172` "정말 제거하시겠습니까?" → "이 워크스페이스를 제거할까요?" (L1) |
| `ko.json` · `en.json` | drift.* 전면 한국어화 (`missing`→"깨진 링크", `stale`→"오래된 참조", `ok`→"동기화됨") · composer.* 재작성 · empty.* 신규 · error.drift.*/error.fs.* 신규 · onboarding.* 신규 (양쪽 locale 동등) |

**DoD**:
- renderer 내 "stale"/"missing"/"drift"/"ref"/"@ref"/"Clear" 하드코딩 영문 **0건** (grep 확인)
- ErrorBoundary 한국어/영문 언어 전환 즉시 반영
- humanizeError 호출부 ≥ 3 (Composer/Drift/Inbox scan/AllProjects scan)

---

### S4 — Performance Structure (S3 후)

**스코프**: C7 · C8 · H7 · H8 · M8 · M10(선택)

| 파일 | 수정 |
|------|------|
| `state/store.ts:165-166` | 내부: `docsByProject: Map<string, Doc[]>` + `frontmatterIndex: { statuses: Set<string>, sources: Set<string> }` + **모듈 스코프 변수** `let cachedFlat: Doc[] = []` (캐시 배열 ref). 액션별 갱신 프로토콜: `appendDocs(docs)` → Map 증분 → `cachedFlat = Array.from(docsByProject.values()).flat()` 재생성 → `set({ docs: cachedFlat, docsByProject: new Map(docsByProject), frontmatterIndex })`. `setDocs`, `removeDocs`, `updateDoc` 모두 동일 순서로 **Map 변경 후 cachedFlat 재생성 후 `set({docs: cachedFlat, ...})` 단일 호출**. `docs` 필드는 레거시 호환 유지(점진 마이그레이션), 새 구독자는 `docsByProject` 직접 사용. (Critic C-2: Zustand 5 는 `set()` 마다 얕은 비교로 notify — `docs` 를 명시적으로 갱신하지 않으면 identity 안정성 불가) |
| **신규** `hooks/useDocs.ts` 확장 | `useDocsOf(projectId)`, `useAllDocsFlat()`, `useFrontmatterIndex()` 3 selector |
| `views/InboxView.tsx:66-134` | 자체 `scanDocs` 루프 삭제 → `useAllDocsFlat()` 구독 + useDocs 스캔 트리거만 유지. deps 에서 `projects` 배열 참조 제거 |
| `views/AllProjectsView.tsx:83,105-108,272` | `storeDocs: s.docs` → `useAllDocsFlat()` · `applyMetaFilter` 는 `useFrontmatterIndex` 에서 statuses/sources 받음 |
| `components/FilterBar.tsx:138-149` | `useMemo allStatuses/allSources` 전체 for-loop 제거 → `useFrontmatterIndex()` |
| `App.tsx:128-137,141-163` | `pruneStaleDocSelection` / 이전 선택 복원 effect 에 `debounce(500ms)` + `docs.length` 안정 체크 (마지막 chunk 이후 500ms 경과 시 1회 실행) |
| `lib/markdown.ts:2` · `MarkdownViewer.tsx:6-8` | `rehype-highlight` / `highlight.js` CSS 를 dynamic import: `MarkdownViewer` 내부 `useEffect(async () => { const [rh, css] = await Promise.all([import('rehype-highlight'), import(theme === 'dark' ? 'highlight.js/styles/github-dark.css' : 'highlight.js/styles/github.css')]); setRehypePlugins([rh.default]); })`. 첫 md 렌더 전까지 plain text. ProjectView chunk 1074KB → ~200KB 예상 |
| `lib/findInContainer.ts:47-81` **선택** (M10) | TreeWalker 를 `requestIdleCallback` chunk(500 nodes/tick)로 분할. 긴 md 에서 INP 개선 |

**DoD**:
- `pnpm bench:transport` 기존 항목 회귀 0
- 실측 벤치 신규(`scripts/bench-renderer-chunks.ts`): 2377 docs × 60 chunks 시뮬 시 `appendDocs` cumulative 시간 **50% 이상 감소**
- InboxView mount 시 project scan IPC 호출 ≤ 1 회 (중복 제거, DevTools Network/IPC 로그)
- ProjectView chunk 사이즈 1074KB → 300KB 이하 (electron-vite build output)
- 기존 `state/store.test.ts` · `docFilters.test.ts` 회귀 0 + 신규 appendDocs Map 테스트 ≥ 3

---

### S5 — SSH 보안 · Dark Pattern 해소 (S3 후)

**스코프**: C9 · H10 · H11 · M4 · M7 · M11

| 파일 | 수정 |
|------|------|
| `main/ipc/workspace.ts:449-466` | `workspace:remove` 핸들러에 `if (target.transport?.type === 'ssh') await removeHostKey(target.id)` 배선 · 활성 SSH 연결이 있으면 `disposeConnection(id)` 호출 |
| `WorkspaceManageModal.tsx` confirm 모달 | SSH workspace 제거 시 "저장된 호스트 지문도 함께 삭제됩니다" 1줄 고지 표시 + M7 "되돌리기" toast 10초(삭제된 workspace 메타 + hostKey 임시 보관) |
| `SshHostKeyPrompt.tsx:233-250` | Trust 분할: "이번 세션만 신뢰" · "영구 신뢰" · "거부" 3 버튼. `rememberHostKey` 체크박스 신설(M11 default=off). verify IPC payload 에 `persistence: 'session'|'permanent'` 필드 추가 |
| `main/transport/ssh/hostKeyDb.ts` | `setHostKey` 에 `{ session?: boolean }` 옵션 — session 키는 **메모리 Map 저장 (key 스키마 = `${hostname}:${port}`, 동일 호스트 여러 workspace 간 신뢰 공유)** (앱 종료 시 휘발). 영구 저장은 기존 `workspaceId` 키. `getHostKey` 조회 시 메모리 Map(host:port) 먼저 확인 후 store(workspaceId) fallback. 만료 옵션은 v0.5. (Critic M-5) |
| `SshHostKeyPrompt.tsx` 버튼 variant | `Reject=primary(destructive)` · `Trust=neutral` (시각 비대칭 해소 — Dark Pattern 평가자 M11 지적) |
| `ComposerTray.tsx:50-68` | 최초 "Copy @ref" 호출 시 1회 dismissible `<StatusMessage>` 경고: "절대 경로가 포함됩니다 — 외부 공유 시 주의" (`prefs.composerPathWarningDismissed` 로 영속) · 토큰 Gauge 옆 "약 $X" 옵트인 표시(`prefs.showTokenCostEstimate`, 기본 off — `tokenEstimate.ts` 확장) |
| `SshWorkspaceAddModal.tsx` 에러 표시 | 각 input 에 `aria-invalid` + `aria-describedby={errorId}` (M4). 에러 메시지에 해결 가이드 추가("포트는 1–65535 범위") |
| `Settings.tsx:25-28` | Experimental SSH OFF 시 "기존 SSH 연결 데이터도 삭제" 체크박스 + `ssh:purge-all` IPC 추가 · 기본 off (사용자 능동 선택) |
| `DriftPanel.tsx` 2MB skip 고지 | S3 에서 추가한 `error.drift.fileTooLarge` 를 DriftPanel 푸터에 "크기 한도 초과로 N 파일 건너뜀" 형태로 집계 표시 (L2) |

**DoD**:
- `workspace:remove` 호출 후 `store.get('sshKnownHosts')[id]` 가 undefined (전용 테스트 추가)
- TOFU 모달에서 "이번 세션만" 선택 후 앱 재시작 시 재질의 발생 (통합 테스트)
- `aria-invalid`/`aria-describedby` 바인딩 수동 확인 (VoiceOver 1패스)
- Composer 최초 복사 시 경고 1회 → dismiss 후 재표시 없음

---

### S6 — SSH Add Modal 2-step Wizard + Focus Trap (S5 후)

**스코프**: H3

| 파일 | 수정 |
|------|------|
| `SshWorkspaceAddModal.tsx:145-157,448-722` | 2-step wizard: Step1 = 이름 + 주소(host:port) + 계정(user) / Step2 = 인증(자동 로그인 / 키 파일) + 원격 폴더 picker. "자동 로그인(권장)" 기본 선택 + 키 파일 섹션은 접힘. "다음"/"이전" 네비 + `Step indicator` (1/2). `config import` 는 Step1 상단. |
| `SshWorkspaceAddModal.tsx` focus trap | 모달 open 시 첫 focusable 에 autofocus + Tab/Shift+Tab 순환 + close 시 trigger 복귀 (`WorkspaceManageModal.tsx:31-60` 패턴 재사용) |
| `SshHostKeyPrompt.tsx:42-50` | 동일 focus trap + trigger 복귀 |

**DoD**:
- Step1 에서 필드 ≤ 4개 (이름·host·port·user)
- Tab 으로 모달 바깥 탈출 불가 (수동 확인)
- Step1→Step2 전환 시 첫 필드 autofocus

---

### S7 — Annotation MVP (#3 하이라이팅, S4 후)

**스코프**: 신규 기능. **로컬 md only, 단일 형광색(노랑), note 없음**.

| 파일 | 수정 |
|------|------|
| **신규** `src/renderer/lib/annotation/anchor.ts` | `dom-anchor-text-quote` + `dom-anchor-text-position` wrapper. `createSelector(range) → {exact, prefix, suffix, start, end}` / `anchorToRange(root, selector) → Range \| null`. 실패 시 orphan flag |
| **신규** `src/renderer/lib/annotation/sidecar.ts` | `loadAnnotations(docPath): Promise<Annotation[]>` / `saveAnnotations(docPath, annotations): Promise<void>` — IPC 경유 |
| **신규** `src/main/ipc/annotation.ts` | `annotation:load`/`annotation:save` handler · sidecar path: `<docPath>.anno.json` · JSON validation(zod schema) · 2MB 상한 · `assertInWorkspace` 가드 |
| **신규** `src/renderer/state/annotationStore.ts` (또는 zustand slice) | `annotationsByDoc: Map<docPath, Annotation[]>`, `addAnnotation`, `removeAnnotation` |
| **신규** `src/renderer/hooks/useAnnotations.ts` | `useAnnotations(docPath)` — 로드 + selection 이벤트 구독 + CSS Highlight 등록 |
| **신규** `src/renderer/components/AnnotationToolbar.tsx` | 텍스트 선택 시 floating toolbar (`selectionchange` + `getBoundingClientRect`). 버튼: 🖍 하이라이트 / ✕ 제거. 좌측 margin dot indicator |
| `MarkdownViewer.tsx` | `useAnnotations(activeDocPath)` + CSS Highlight 등록 (`CSS.highlights.set('markwand-annotation-highlight', highlight)`). pointer-events 우회를 위해 `onClick` 시 `caretRangeFromPoint` 로 역조회하여 hit annotation 판별 |
| `lib/markdown.ts` | sanitizeSchema **무변경** (CSS Highlight 채택으로 DOM 미주입) |
| `styles/globals.css` | `::highlight(markwand-annotation-highlight) { background-color: rgba(255, 213, 79, 0.5); }` 추가 (다크 토큰 버전도) |
| `preload/index.ts` + `preload/types.ts` | `window.api.annotation.{load, save}` 노출 |
| `package.json` | `dom-anchor-text-quote` + `dom-anchor-text-position` 신규 의존성 (~5KB) |
| `ko.json` · `en.json` | annotation.* 키셋 (툴바 라벨, orphan 안내) |

**Annotation JSON schema** (zod):
```ts
{
  version: 1,
  annotations: [{
    id: string,                   // uuid
    selector: { type: 'TextQuote', exact, prefix, suffix },
    positionFallback: { start: number, end: number },
    color: 'yellow',              // v0.4 single
    createdAt: ISO string,
    orphan?: boolean              // runtime flag
  }]
}
```

**DoD**:
- 3 개 md 파일에서 텍스트 선택 → 하이라이트 → 앱 재시작 → 하이라이트 복원 (수동 시나리오)
- 원본 md 1 byte 편집 후에도 text-quote fuzzy 매칭 성공 (**anchor.test.ts 케이스 ≥ 9: heading · inline code · code fence · mermaid block · GFM table 셀 · HTML raw block · footnote `[^1]` · 한국어 CJK · surrogate pair/이모지**) (Critic M-4)
- 하이라이트 100개 적용된 md 렌더 FPS 회귀 ≤ 10%
- orphan 발생 시 사이드바 뱃지 "복구 필요 N" 표시 + **Success Metric: orphan 비율 ≤ 5%** (anchor.test.ts 케이스 기준)
- SSH workspace 에서는 annotation UI 비활성(툴바 disabled) + tooltip "원격 문서는 v0.5 지원"
- `lib/highlightTheme.ts` / annotation CSS 주입은 **내부 상수만 사용** — 사용자 입력 색상 허용 시 sanitize 필수 (v0.5 확장 시 재검토, Critic m-4)
- **Sidecar JSON `version: 1` schema** — v0.5 확장 시 `version: 2` + migration 스크립트(`.md.anno.json` 일괄 변환)를 `scripts/migrate-annotations.ts` 로 제공 (Critic m-5)

**Scope OUT (v0.5+)**: 다색 팔레트 · note 텍스트 popover · annotation 검색 · SSH 쓰기 · `.md` footnote 내보내기

---

### S8 — SSH 원격 프로젝트 자동 갱신 (독립)

**스코프**: Known Gap (NOVA-STATE 이관 항목)

| 파일 | 수정 |
|------|------|
| `main/transport/ssh/watcher.ts` (SshPoller) | 디렉토리 레벨 polling 추가 — 현재 30s/60s 문서 레벨 poll 에 **depth ≤ 2 readdir diff** 확장. 변경 감지 시 `EventEmitter.emit('project-change')` |
| `main/transport/ssh/index.ts:161` | `transport:status` 외 **기존 `fs:project-change` 채널 재사용** IPC 송신 추가 (renderer useDocs/refreshKey 자동 연동 — **신규 IPC 채널 0**, App.tsx:250-265 기존 핸들러 재사용) (Critic m-3 Q5 근거 구체화) |
| `services/watcher.ts` payload 구조 | 로컬 `fs:project-change` 와 동일 스키마 유지 — renderer 는 출처 불문 동일 처리. payload 가 현재 빈 객체라 추가 수정 없음 |

**DoD**:
- Docker sshd 통합 테스트: 원격 디렉토리 생성/삭제 시 renderer 가 60초 내 반영 (기존 integration-ssh.ts 확장)
- 기존 문서 레벨 poll 회귀 0

---

### S9 — Release v0.4.0-beta.1

| 항목 | 작업 |
|------|------|
| Version | `package.json` 0.3.0-beta.9 → `0.4.0-beta.1` |
| Release notes | `docs/release-notes/v0.4.0-beta.1.md` — UX Overhaul 7 축 요약 + annotation MVP + SSH 자동 갱신 |
| Build | `pnpm dist:mac` arm64 + x64 |
| SHA256 | `install-macos.md` / `install-macos.en.md` 기입 |
| GitHub Release | tag push + DMG upload + changelog |
| CLAUDE.md / README | v0.4 기능 반영 |

---

## Risk Map

| 위험 | 심각도 | 완화 |
|------|--------|------|
| **C7 Map 전환 점진 마이그레이션 중 구독자 누락** — `s.docs` 캐시 reference identity 가 깨져 무한 렌더 | High | `useSyncExternalStoreWithSelector` + cached array ref. Explorer C 지적: Zustand shallow 불가. **단일 selector 모듈**로 격리 + 단위 테스트 5건(append/update/remove/reset/order) |
| **S7 annotation sidecar write 실패 silent** — electron FS 권한 · read-only mount | Medium | `fs.access(W_OK)` 선검사 → 실패 시 toast + 사이드바 disabled. `assertInWorkspace` 이중 가드 |
| **text-quote 매칭 실패율** — rehype-sanitize 후 DOM 텍스트 vs raw md 불일치 (가령 footnote, mermaid placeholder) | Medium | anchor 생성은 **렌더된 DOM 기준** (Range.toString()). 테스트 케이스: headings/code block/inline code/mermaid block 각각 |
| **C9 GDPR 수정 시 기존 설치자 잔존 host key** | Medium | v0.4 startup 1회 orphan sweep: `sshKnownHosts` 키 중 workspace 미존재 항목 자동 삭제 + log 기록 |
| **H8 rehype-highlight dynamic 중 flash** — 첫 md 렌더 시 plain text → highlighted 전환 시 1프레임 깜빡임 | Low | `<pre>` 에 monospace font-family 미리 적용 + opacity 0→1 fade 80ms (이미 `--duration-fast`) |
| **S6 2-step wizard → 사용자 학습 비용** — 기존 한 화면 익숙한 사용자 혼란 | Low | Step indicator(1/2) + "이전" 상시 enabled + 기존 필드 값 유지. 모달 닫고 재진입 시 마지막 Step 복원 |
| **S7 CSS Highlight pointer-events 제한** — 클릭 이벤트 못 받음 | Medium | `caretRangeFromPoint(x, y)` 역조회 — 이미 `findInContainer.ts` 에 유사 패턴. hover tooltip 은 mousemove throttle 16ms |
| **S8 SSH readdir depth-2 부하** — 큰 원격 워크스페이스(예: 50 프로젝트 × 깊은 트리) 60초 주기 부담 | Low | 이미 30s/60s 2구간 polling — 디렉토리 레벨은 루트 직속만(depth=1) 우선. depth=2 는 옵션 |

---

## Unknowns

1. **react-markdown 10 의 `sourcePos`** 가 text-quote 생성 시 유용한지 — Explorer B 는 직접 활용 불필요라고 판정했으나 orphan 복구 UX 에서 소스 위치 기반 힌트 제공 가능. S7 구현 중 prototyping 필요.
2. **CSS Highlight API 의 overlap annotation 렌더 순서** — 100+ annotation 겹침 시 색상 누적 규칙(Chromium 130 의 실제 동작). 필요 시 투명도 감소 또는 single-highlight + span fallback 혼합. (Critic OR-2)
3. **electron-vite 의 rehype-highlight dynamic import 청크 분리 동작** — vite `manualChunks` 튜닝 필요 여부. S4 빌드 결과 번들 analyzer 실측.
4. **GDPR 자동 orphan sweep** — Critic m-2 반영: 사용자 동의 없이 삭제 대신 "startup 1회 toast + 3초 지연 + undo 버튼"으로 변경. 삭제 전 `~/Library/Application Support/Markwand/ssh-orphan-backup-<date>.json` 백업 자동 기록(7일 보관 후 삭제).
5. **C4 FilterBar 접기 상태에서 "활성 필터 N 개" 배지 클릭 시 동작** — 펼치기만? 또는 전체 리셋? 디자인 판단 S2 구현 중 결정.
6. **CSS `attr()` advanced syntax 미지원 대응** (Critic C-1) — Electron 업그레이드 시(33→35+) Chromium 133+ 진입하면 `attr(width)`/`attr(height)` 사용 가능. 그때 markdown.css 의 `aspect-ratio: attr(width)/attr(height)` 활성화 여부 재검토.

---

## Verification Hooks

### Sprint 별 Evaluator 체크리스트

| Sprint | Evaluator subagent | 핵심 검증 |
|--------|-------------------|-----------|
| S1 | nova:qa-engineer | WCAG 대비 재계산(`#1f6feb` on white) · CLS ≤ 0.1 · RecentDocsPanel 자정 재스케줄 |
| S2 | nova:qa-engineer | 키보드 리사이즈 E2E · axe 위반 0 · sm 버튼 hit-area 28px · aria-live 실제 낭독 |
| S3 | nova:senior-dev | grep 하드코딩 영문 0 · humanizeError 호출부 ≥ 3 · ErrorBoundary i18n 전환 |
| S4 | nova:senior-dev | bench `appendDocs` 50%↓ · InboxView IPC 중복 제거 · ProjectView chunk <300KB · identity 안정성 unit test |
| S5 | nova:security-engineer | workspace 제거 후 hostKeyDb orphan 0 · session-only TOFU 재시작 시 재질의 · prefs purge 옵션 동작 |
| S6 | nova:qa-engineer | Step1 필드 수 · Tab trap · autofocus · Escape 동작 |
| S7 | nova:qa-engineer | 하이라이트 복원 · 1 byte 편집 후 fuzzy 매칭 · 100 annotation FPS · orphan UI |
| S8 | nova:qa-engineer | Docker sshd 원격 mkdir/rmdir 감지 시간 · 기존 poll 회귀 0 |
| S9 | nova:senior-dev | SHA256 일치 · DMG 설치·기동 · release notes 정확 |

### 자동화 게이트
- `pnpm typecheck && pnpm lint` — 전 sprint 필수
- `pnpm test` — 회귀 0 (기존 250/260 baseline)
- `pnpm bench:transport` — baseline.json 대비 +3% 이내
- **신규**: `pnpm bench:renderer-chunks` (S4) · `pnpm test -- annotation` (S7) · `pnpm test:integration-ssh` (S8)

### `bench:renderer-chunks` 측정 프로토콜 (Critic m-1)
- 입력: 2377 docs × 60 chunks × 39.6 docs/chunk (실 워크스페이스 재현)
- 측정 방식: 순수 JS (store.ts 액션 직접 호출, IPC 미포함 — renderer 측 구조 변경 isolate)
- Warm-up: 3회 폐기 + 본 측정 10회
- GC: `global.gc()` 강제(노드 `--expose-gc`) 각 iteration 사이
- 기록: p50/p95/p99/stdDev + cumulative `appendDocs` 시간, `docsByProject.get()` 시간, frontmatterIndex build 시간
- Baseline: 0.3.0-beta.9 현 구조 (flat spread) 측정치 `scripts/bench-renderer-chunks.baseline.json` 에 기록 → S4 완료 후 비교
- DoD "50%↓" 는 **cumulative appendDocs 시간 p95** 기준

### 하드 게이트
- 커밋 전 `/nova:review --fast` 통과
- 각 스프린트 독립 Evaluator PASS 없이 다음 sprint 진입 금지
- NOVA-STATE.md Last Activity 에 "→ PASS" 명시

---

## Open Risks (사용자 판단 필요 — Plan 자동 반영 불가)

### OR-1 sidecar JSON → v0.5 SQLite 전환 비용 (Critic)
- v0.5 에 annotation 검색/note 패널 추가 시 파일당 JSON 쿼리는 O(N) — 수천 annotation 누적 시 부담
- 마이그레이션: `.md.anno.json` 일괄 읽기 → SQLite 로 import. 사용자 당 수초~수십 초 1회
- **결정 필요**: v0.4 에서 SQLite 선도입할지, sidecar 로 MVP 후 v0.5 전환 수용할지

### OR-2 CSS Highlight overlap 렌더 동작 (Critic)
- Chromium 130 에서 100+ 겹침 시 색상 누적 규칙 스펙 미정의
- **완화책 (v0.4 MVP)**: single highlight 이름만 사용(`markwand-annotation-highlight`) · 겹침 발생 시 경고 미표시(단색이므로 시각 차이 없음) · v0.5 다색 도입 시 PoC 필수

### OR-3 분할 릴리스 여부 (Critic M-2)
- **옵션 A**: v0.4.0-beta.1 (S1~S6 UX Audit 해소) → 사용자 dogfood → v0.4.0-beta.2 (S7 annotation + S8)
- **옵션 B**: v0.4.0-beta.1 (S1~S8 전체 단일 릴리스)
- **사용자 판단 포인트**: 옵션 A 는 UX 개선 피드백을 annotation 설계에 반영 가능, 옵션 B 는 릴리스 오버헤드 단축

### OR-4 renderer 컴포넌트 테스트 인프라 부재 (Critic M-3)
- 현재 vitest node env — RTL/jsdom 미구성. 신규 UI(I18nErrorBoundary · AnnotationToolbar · 2-step wizard · FilterBar collapse) 자동 검증 불가
- **옵션 A**: S0 sprint 선행 — jsdom + RTL + happy-dom 중 택 1 설치, 기존 state/store.test 영향도 확인
- **옵션 B**: 수동 E2E 체크리스트로 대체 (각 DoD 의 수동 확인 항목 강화)
- **권장**: 옵션 A (S0, 0.5d) — 이후 S3/S6/S7 DoD 에 컴포넌트 테스트 1~2 건 추가

### OR-5 annotation MVP "1색" 충분성 (Critic m-5)
- 피드백 #3 "형광펜" 의 기대가 다색인지 단색인지 미확인
- **권장**: v0.4 MVP 노랑 1색 진행 → 릴리스 후 사용자 반응 기반 v0.5 다색 추가

## Scope Guard

| 항목 | 결정 |
|------|------|
| v1.0 SSH IPC 분기 (PR-A, 기존 M-2) | **OUT** — v0.3 Known Gap 유지 |
| SSH annotation 쓰기 | **OUT** — S7 은 로컬 md only |
| Annotation 4색 · note · 검색 | **OUT** — v0.5+ |
| .md footnote 내보내기 | **OUT** — v0.5+ |
| Windows/Linux 빌드 | **OUT** — v1.0 |
| `project:scan-docs` transport 분기 (RM-7) | **OUT** — v1.0 |
| Annotation 외 SSH S8 | **IN** (Q5 결정) — SshPoller 내부 확장만 |

---

## Success Metrics

- **UX Audit 재실행** (릴리스 직전): Critical 0 · High ≤ 3 (잔존 v0.5 이관) · Medium ≤ 6
- **ProjectView chunk 사이즈**: 1074KB → ≤ 300KB
- **appendDocs cumulative** (2377 docs × 60 chunks 시뮬): 현재 기준 50%↓
- **WCAG 2.2 AA**: 다크 탭·Button sm·DriftBadge·focus-visible 전부 4.5:1 이상
- **첫 문서 열기까지 클릭 수**: 워크스페이스 추가 → 프로젝트 → 문서 = **≤ 3** (변경 없음 유지)
- **비개발자 테스트**: 외부 PM 1 인에게 empty state → 첫 문서 열기 시도, 음성 피드백 수집 (릴리스 전 옵션)

---

## 부록 A — 사용자 피드백 매핑

| 사용자 원문 | 해소 sprint |
|-------------|-------------|
| #3 "문서 하이라이팅? 형광펜? 내부 그리기?" | **S7** annotation MVP |
| #4 "이미지 4 ASCII 박스 그림 개선" | **S1** (CLS + aspect-ratio + mermaid min-height) — 여백/jump 해소 |
| #6 "현재 뷰어도 좋지만 더 이쁘게" | **S1+S2+S3+S4** (토큰/레이아웃/카피/성능) 종합 |
| #7 "개발자/비개발자 구분없는 UX/UI" | **S3** (i18n·humanizeError·onboarding) 중심 + S2·S5 보조 |

## 부록 B — 파일 교집합 매트릭스 요약

중복 ≥ 3 sprint 파일 (순차 필요): `FilterBar.tsx` (S2·S3·S4) · `ComposerTray.tsx` (S2·S3·S5) · `DriftPanel.tsx` (S1·S3·S5) · `MarkdownViewer.tsx` (S1·S4·S7) · `ko.json`/`en.json` (S3·S5·S6·S7)

중복 = 2 sprint: `App.tsx` (S3·S4) · `SshHostKeyPrompt.tsx` (S5·S6) · `SshWorkspaceAddModal.tsx` (S5·S6)

**병렬 가능 쌍**: S1 ∥ S2 · S8 은 언제든

---

## Next Step

### 착수 전 사용자 승인 (하드 게이트 — Critic M-2 반영)
1. **Q1-Q5 결정 확인** — sidecar JSON · 원본 옆 · text-quote 중심 · drift 문제 시에만 표시 · SSH S8 v0.4 포함
2. **annotation 범위 확정 (Critic m-5)** — 노랑 1색 · note 없음 · 로컬 md only · SSH annotation v0.5+. 이 결정 없이 S7 착수 금지
3. **OR-3 분할 릴리스 선택** — 옵션 A(beta.1 UX + beta.2 annotation) vs 옵션 B(beta.1 단일)
4. **OR-4 테스트 인프라** — S0 RTL/jsdom 선행 sprint 추가 여부

### 이후 실행 체인
5. (선택) `/nova:design` — S7 Annotation MVP text-quote anchor 알고리즘 + sidecar JSON zod schema 세부
6. `/nova:auto` 또는 sprint 단위 `/nova:run` — S1 부터 DAG 순서대로 착수

---

## Refiner 기록 (Critic 반영 내역)

| Critic 지적 | 반영 위치 | 수정 |
|-------------|----------|------|
| C-1 CSS `attr()` Chromium 130 미지원 | S1 markdown.css · MarkdownViewer.tsx · DoD | `aspect-ratio: attr()` 규칙 제거, JS `onLoad` 단일 경로로 확정 + Electron 33 실측 DoD 추가 |
| C-2 Zustand identity 전략 미서술 | S4 store.ts | `cachedFlat` 모듈 스코프 변수 + 액션별 갱신 프로토콜 (Map 변경 → cachedFlat 재생성 → `set({docs, docsByProject, frontmatterIndex})` 단일 호출) 명시 |
| M-1 I18nErrorBoundary 패턴 | S3 | class 가 5개 label prop 받고 함수형 래퍼가 `useTranslation` 으로 바인딩, `getDerivedStateFromError` class 내부 유지 명시 |
| M-2 분할 릴리스 판단 | Open Risks OR-3 + Next Step 하드 게이트 | 사용자 판단 포인트로 명시, 사용자 승인 없이 S7 착수 금지 |
| M-3 renderer 테스트 인프라 부재 | Open Risks OR-4 | S0 RTL/jsdom 선행 sprint 옵션 A / 수동 E2E 옵션 B 제시 |
| M-4 anchor.test.ts 케이스 부족 | S7 DoD | 9 케이스로 확장 (heading/inline code/code fence/mermaid/GFM table/HTML raw/footnote/CJK/surrogate) + orphan 5% SLO |
| M-5 session TOFU Map key 스키마 | S5 hostKeyDb | session key = `${hostname}:${port}` (동일 호스트 여러 workspace 간 공유), 영구는 `workspaceId` 명시 |
| m-1 bench 프로토콜 | Verification Hooks | 측정 방법(순수 JS/IPC 제외) · warm-up 3 + 본 10 · `--expose-gc` · p95 기준 SLO 명시 |
| m-2 GDPR sweep undo | Unknowns #4 | 즉시 삭제 → "toast + 3초 지연 + undo" + 7일 백업 JSON 로 변경 |
| m-3 Q5 Scope Guard 일관성 | S8 설계 | "기존 `fs:project-change` 채널 재사용, 신규 IPC 0" 명시 |
| m-4 highlightTheme XSS 표면 | S7 DoD | "내부 상수만 사용, 사용자 입력 색상 허용 시 sanitize 필수" 주석 |
| m-5 schema versioning | S7 DoD | `version: 1` sidecar + v0.5 `version: 2` 마이그레이션 스크립트 전략 명시 |
